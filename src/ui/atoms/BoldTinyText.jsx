export const BoldTinyText = ({text}) => {

    return(
        <div>
        <div style={{
            fontSize:"13px",
            fontWeight:"bold",
        }}>
            <p>{text}</p>
           </div>
           
         </div>
    )
}